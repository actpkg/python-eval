[project]
name = "python-eval"
version = "0.1.1"
description = "Python interpreter"
requires-python = ">=3.14"
dependencies = [
    "act-sdk>=0.1.1",
]

[dependency-groups]
dev = [
    "componentize-py>=0.21.0",
    "ruff>=0.15.8",
]
