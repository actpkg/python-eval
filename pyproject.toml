[project]
name = "python-eval"
version = "0.2.0"
description = "Python interpreter"
requires-python = ">=3.14"
dependencies = [
    "act-sdk",
    "cbor2<6.0",
]

[dependency-groups]
dev = [
    "componentize-py>=0.21.0",
    "ruff>=0.15.8",
]

[tool.uv.sources]
act-sdk = { path = "../../act-sdk-py" }
